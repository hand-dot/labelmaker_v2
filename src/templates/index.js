import {
  tablecolumns, dataschema, base64, position, sampledata,
} from './letterpack';

export default {
  letterpack: {
    columns: tablecolumns, dataSchema: dataschema, image: base64, position, sampledata,
  },
};
